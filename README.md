# Background Remover API (FastAPI + rembg)

A simple API to remove image backgrounds using `rembg`.

## 🚀 Deployment on Railway
1. Clone this repo:
   ```sh
   git clone https://github.com/YOUR_USERNAME/rembg-api.git
   cd rembg-api
   ```
